package com.zea.company.route_u.model;

public class Open_now {
    boolean open_now;

    public Open_now(boolean open_now) {
        this.open_now = open_now;
    }

    public boolean isOpen_now() {
        return open_now;
    }

    public void setOpen_now(boolean open_now) {
        this.open_now = open_now;
    }
}
